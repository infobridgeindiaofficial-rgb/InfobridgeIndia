import{acceptCompanyMemberInvitation}from"/company/member-invitations.js";import{clearCompanyCache,currentCompany,currentUser}from"/supabase/client.js";
const token=new URLSearchParams(location.search).get("token")||"",intro=document.querySelector("[data-member-invite-intro]"),error=document.querySelector("[data-member-invite-error]"),success=document.querySelector("[data-member-invite-success]"),auth=document.querySelector("[data-member-invite-auth]"),panel=document.querySelector("[data-member-invite-accept]"),button=document.querySelector("[data-accept-company-member]");
const fail=message=>{error.textContent=message;error.hidden=false};
if(!token){intro.textContent="This invitation link is incomplete.";fail("Invitation token is missing.")}else{const user=await currentUser();if(!user){intro.textContent="Authenticate with the invited email address to continue.";auth.hidden=false;const encoded=encodeURIComponent(token);document.querySelector("[data-member-invite-login]").href=`/login.html?memberInvite=${encoded}`;document.querySelector("[data-member-invite-signup]").href=`/signup.html?memberInvite=${encoded}`}else{intro.textContent="Confirm that you want to join this company as a Company Member.";panel.hidden=false;document.querySelector("[data-member-invite-email]").textContent=user.email||"authenticated account";button.onclick=async()=>{button.disabled=true;error.hidden=true;try{await acceptCompanyMemberInvitation(token);clearCompanyCache();const company=await currentCompany();success.textContent=`Invitation accepted. You joined ${company?.name||"the company"} as Company Member.`;success.hidden=false;panel.hidden=true;setTimeout(()=>location.replace("/index.html"),900)}catch(cause){fail(cause.message||"Unable to accept this invitation.");button.disabled=false}}}}
