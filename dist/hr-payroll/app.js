import{STORES,uid,attendanceBelongsToEmployee,attendanceFacts,attendanceMonthDetail,attendanceSummary,validateAttendance,workingDates,periodCalendar,monthPeriod,weeklyOffConfig,validateWeeklyOff,WEEKDAYS,toCsv,parseCsv}from"./core.js";import{createWorkspaceStore}from"/supabase/workspace.js";import{workbookDatasets}from"/banking-workspace/xlsx.js";import{repository as financeRepository}from"/finance-workspace/repository.js";import{saveDraft as saveFinanceDraft}from"/finance-workspace/core.js";import{XLSX}from"/gst-workspace/xlsx-shim.js";import{absorbLegacyDepartments,sharedDepartments}from"/administration-workspace/departments.js";import{currentCompanyName,listenForAdministrationCompany}from"/administration-workspace/company.js";import{employeeCountryCode,formatHrMoney,hrSetupDefaults,payrollForCountryEmployee,prepareEmployeeRecord,resolveEmployeeCountryConfig,resolveHrCountryConfig}from"./country.js";import{employeeImportErrorRows,employeeTemplateWorkbookData,validateEmployeeImportRows}from"./employee-import.js";import{resetEmployeeDataEverywhere}from"./employee-reset.js";import{companyPayslipSnapshot,renderPayslipDocument}from"./payslip.js";import{attendanceImportRecordId,saveAttendanceImport}from"./attendance-import.js";import{attendanceImportProgressController}from"./attendance-import-ui.js";import{canonicalPayrollPeriod,payrollAttendanceIssues,payrollIssueSummary,repairDraftPayrollRun}from"./payroll-period.js";import{recoverLegacyHrFallback}from"./fallback-recovery.js";import{approvePayrollAndGeneratePayslips}from"./payroll-approval.js";import{availableLeaveTypes,employeeLeaveBalances,leaveTypeAllowedDays,leaveTypeIsActive,leaveTypeIsPaid,validateLeaveApplication}from"./leave.js";import{lockedPayrollForLeave}from"./leave-payroll-lock.js";import{reportCsv,reportMonthLabel,summarizeHrReport}from"./reports.js";import{companySecurityStatus,executeHrReset}from"../security/client.js";import{brandedWorkbook}from"../export/workbook.js";const allowedViews=new Set(["dashboard","employees","attendance","leave","payroll","payslips","reports","settings"]);let store,state={},view=allowedViews.has(location.hash.slice(1))?location.hash.slice(1):"dashboard",importRows=[],employeeImportResults=[],employeeImportCountry="IN",employeeCountryFilter="all",employeeDepartmentFilter="all",employeeStatusFilter="all";let attendancePeriod={month:""},leaveRequestPage=1,leaveSearch="",leaveStatusFilter="all",editingLeaveId="";const $=s=>document.querySelector(s),esc=v=>String(v??"").replace(/[&<>"']/g,(c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])));const companyDisplayName=()=>currentCompanyName(window.InfoBridgeCompany?.name||state.settings?.[0]?.displayName||state.settings?.[0]?.companyName||"Company");const hrCountry=()=>resolveHrCountryConfig(window.InfoBridgeCompany),money=value=>formatHrMoney(value,window.InfoBridgeCompany),payrollForEmployee=(employee,inputs)=>payrollForCountryEmployee(employee,inputs,window.InfoBridgeCompany);const icons={dashboard:"▦",employees:"♙",attendance:"◷",leave:"◇",payroll:"₹",payslips:"▤",reports:"▥",settings:"⚙"};const localCacheKey=()=>`infobridgeindia.hr-payroll.fallback.v1:${window.InfoBridgeUser?.id||"local"}:${window.InfoBridgeCompany?.companyId||"company"}`;const readLocalCache=()=>{try{return JSON.parse(localStorage.getItem(localCacheKey())||"{}")||{}}catch{return{}}};const writeLocalCache=value=>localStorage.setItem(localCacheKey(),JSON.stringify(value));function localFallbackStore(){return{offline:true,async all(collection){return readLocalCache()[collection]||[]},async put(collection,value){const cache=readLocalCache(),rows=cache[collection]||[],index=rows.findIndex((row=>String(row.id)===String(value.id)));index<0?rows.push(value):rows[index]=value;cache[collection]=rows;writeLocalCache(cache);return value},async putMany(collection,values,{batchSize:batchSize=500,onProgress:onProgress=()=>{}}={}){let saved=0;for(let start=0;start<values.length;start+=batchSize){const cache=readLocalCache(),rows=cache[collection]||[],chunk=values.slice(start,start+batchSize);for(const value of chunk){const index=rows.findIndex((row=>String(row.id)===String(value.id)));index<0?rows.push(value):rows[index]=value}cache[collection]=rows;writeLocalCache(cache);saved+=chunk.length;onProgress(saved,values.length);if(saved<values.length)await new Promise((resolve=>setTimeout(resolve,0)))}return{saved:saved,batches:Math.ceil(values.length/batchSize)}},async remove(collection,id){const cache=readLocalCache();cache[collection]=(cache[collection]||[]).filter((row=>String(row.id)!==String(id)));writeLocalCache(cache)},async clear(collection){const cache=readLocalCache();cache[collection]=[];writeLocalCache(cache)},async replace(collection,rows){const cache=readLocalCache();cache[collection]=rows;writeLocalCache(cache)}}}async function openDb(){store=await createWorkspaceStore("hr-payroll")}const all=s=>store.all(s);const put=(s,v)=>store.put(s,v);const del=(s,id)=>store.remove(s,id);async function recoverPersistedFallback(){const result=await recoverLegacyHrFallback({cloudStore:store,localData:readLocalCache(),collections:STORES});console.info("InfoBridge HR persistence diagnostics",{authenticatedUserId:store.user?.id||window.InfoBridgeUser?.id||"",selectedCompanyId:window.InfoBridgeCompany?.companyId||"",saveCompanyId:store.company?.id||"",loadCompanyId:store.company?.id||"",cloudEmployees:result.cloudCounts.employees||0,localFallbackEmployees:result.localCounts.employees||0,finalEmployees:result.finalCounts.employees||0,recoveredLegacyFallback:result.recovered});return result}async function load(){for(const s of STORES)state[s]=await all(s);state.leaveTypes=availableLeaveTypes(state.leaveTypes);state.departments=sharedDepartments({includeIds:(state.employees||[]).map((e=>e.departmentId))});if(!store.offline)writeLocalCache(Object.fromEntries(STORES.map((name=>[name,state[name]]))))}async function migrateDepartments(){const employees=await all("employees"),referenced=new Set(employees.map((employee=>employee.departmentId)).filter(Boolean)),legacy=(await all("departments")).filter((department=>referenced.has(department.id)));absorbLegacyDepartments(legacy)}async function ensureHrSettings(){const company=window.InfoBridgeCompany;if(!company?.companyId)throw Error("Complete Company Setup before opening HR & Payroll.");if(state.settings?.length){const settings=state.settings[0];if(settings.companyId!==company.companyId){await put("settings",{...settings,companyId:company.companyId,updatedAt:(new Date).toISOString()});await load()}return}await put("settings",{id:"business",companyId:company.companyId,companyName:company.legalName||company.name,displayName:company.name,...hrSetupDefaults(company),financialYearStart:(new Date).toISOString().slice(0,7),payrollDay:28,payFrequency:"monthly",salaryDays:30,shiftStart:"09:30",shiftEnd:"18:00",standardHours:8,graceMinutes:10,weeklyOff:"Sunday",leaveYearStart:(new Date).toISOString().slice(0,7),setupComplete:true,createdAt:(new Date).toISOString()});await load()}function toast(m,error=false){const e=document.createElement("div");e.className=`toast${error?" error":""}`;e.textContent=m;$("#toast-region").append(e);setTimeout((()=>e.remove()),3500)}function download(name,text,type="text/csv"){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type:type}));a.download=name;a.click();setTimeout((()=>URL.revokeObjectURL(a.href)),1e3)}const date=()=>(new Date).toISOString().slice(0,10),month=()=>date().slice(0,7);let reportPeriod=month();function modal(title,body,onSubmit,wide=false,submitLabel="Save"){$("#modal-root").innerHTML=`<div class="modal-backdrop"><div class="modal ${wide==="xl"?"wide xl":wide?"wide":""}"><header><h2>${title}</h2><button class="icon-button" data-close>×</button></header><form id="modal-form"><div class="modal-body">${body}</div><footer><button type="button" class="btn secondary" data-close>Cancel</button><button type="submit" class="btn primary" data-modal-submit>${submitLabel}</button></footer></form></div></div>`;document.querySelectorAll("[data-close]").forEach((x=>x.onclick=()=>$("#modal-root").innerHTML=""));$("#modal-form").onsubmit=async e=>{e.preventDefault();try{const close=await onSubmit(Object.fromEntries(new FormData(e.target)),e.target);if(close===false)return;$("#modal-root").innerHTML="";await load();render();toast("Saved successfully")}catch(x){toast(x.message,true)}}}function field(n,l,type="text",value="",extra=""){return`<label>${l}<input class="input" name="${n}" type="${type}" value="${esc(value)}" ${extra}></label>`}function select(n,l,opts,value=""){if(n==="departmentId")opts=opts.filter((([id])=>!id||id==value||state.departments.find((department=>department.id==id))?.active!==false));return`<label>${l}<select class="input" name="${n}">${opts.map((([v,t])=>`<option value="${esc(v)}" ${v==value?"selected":""}>${esc(t)}</option>`)).join("")}</select></label>`}function weeklyOffFields(employee={}){const config=weeklyOffConfig(employee,state.settings?.[0]||{});return`${select("weeklyOffType","Weekly-off type",[["one","One day per week"],["two","Two days per week"],["custom","Custom"]],config.type)}<label>Weekly-off days<select class="input" name="weeklyOffDays" multiple size="7" required>${WEEKDAYS.map((day=>`<option value="${day}" ${config.days.includes(day)?"selected":""}>${day}</option>`)).join("")}</select></label><p class="form-note error-text" data-weekly-off-error aria-live="polite"></p>`}async function recalculateDraftPayrolls(){await load();for(const run of state.payrollRuns.filter((item=>item.status==="draft"))){const{from:from,to:to}=canonicalPayrollPeriod(run.period),items=state.employees.filter((e=>e.active!==false)).map((e=>payrollForEmployee(e,{attendance:state.attendance,leaves:state.leaveTransactions,adjustments:state.payrollAdjustments,settings:state.settings[0],from:from,to:to})));await put("payrollRuns",{...run,from:from,to:to,items:items,employeeCount:items.length,totalNet:items.reduce(((sum,item)=>sum+item.net),0),recalculatedAt:(new Date).toISOString()})}await load()}async function repairStoredDraftPayrollPeriods(){let changed=false;for(const run of state.payrollRuns.filter((item=>item.status==="draft"))){const repaired=repairDraftPayrollRun(run,{employees:state.employees,attendance:state.attendance,leaves:state.leaveTransactions,adjustments:state.payrollAdjustments,settings:state.settings[0],calculate:payrollForEmployee});if(repaired.changed){await put("payrollRuns",repaired.run);changed=true}}if(changed)await load();return changed}function setup(){const c=hrCountry(),india=c.country==="IN",statutory=india?`${field("pan","PAN")}${field("tan","TAN")}${field("gstin","GSTIN")}${field("pfNumber","PF registration")}${field("esiNumber","ESI registration")}${field("professionalTax","Professional tax registration")}`:"";return`<main class="setup-shell"><section class="setup-card"><a href="/index.html"><img class="setup-logo" src="/infobridgeindia-logo.png" alt="InfoBridgeIndia"></a><span class="eyebrow">HR & Payroll setup</span><h1>Set up your people workspace</h1><p>Your HR and payroll records use the authenticated ${esc(c.countryName)} company workspace.</p><form id="setup-form"><div class="form-grid"><label>Company<input class="input" value="${esc(companyDisplayName())}" readonly></label><label>Country<input class="input" value="${esc(c.countryName)}" readonly></label><label>Currency<input class="input" value="${esc(c.currency)}" readonly></label><p class="form-note"><a href="/app/admin.html">Edit company name in Administration</a></p>${statutory}${field("financialYearStart","Financial year start","month",month(),"required")}${field("payrollDay","Payroll day","number","28","min=1 max=31 required")}${select("payFrequency","Pay frequency",[["monthly","Monthly"],["weekly","Weekly"],["fortnightly","Fortnightly"]])}${field("salaryDays","Salary days divisor","number","30","min=1 required")}${field("shiftStart","Default shift starts","time","09:30","required")}${field("shiftEnd","Default shift ends","time","18:00","required")}${field("standardHours","Standard daily hours","number","8","step=.25 required")}${field("graceMinutes","Late grace minutes","number","10","min=0")}${field("weeklyOff","Weekly off","text","Sunday")}${field("leaveYearStart","Leave year start","month",month(),"required")}</div><div class="setup-actions"><button class="btn primary">Complete setup</button></div></form></section></main>`}function shell(content){const nav=[["dashboard","Dashboard"],["employees","Employees"],["attendance","Attendance"],["leave","Leave"],["payroll","Payroll"],["payslips","Payslips"],["settings","Settings & Backup"]];return`<div class="app-shell"><aside class="sidebar"><a class="sidebar-brand" href="/index.html"><img src="/infobridgeindia-logo.png" alt="InfoBridgeIndia"></a><div class="workspace-label"><strong>HR & Payroll</strong><span>${esc(companyDisplayName())}</span></div><nav>${nav.map((([v,t])=>`<button class="nav-link ${view===v?"active":""}" data-view="${v}"><span>${icons[v]}</span>${t}</button>`)).join("")}</nav><div class="local-note">Local-first workspace<br><span>Data stays in this browser</span><a href="/index.html">← InfoBridgeIndia home</a></div></aside><main class="main"><header class="topbar"><button class="icon-button mobile-menu">☰</button><label class="global-search">⌕<input id="search" placeholder="Search this workspace"></label></header><div class="content">${content}</div></main></div>`}const head=(title,desc,actions="")=>`<div class="page-head"><div><h1>${title}</h1><p>${desc}</p></div><div class="page-actions">${actions}</div></div>`;const empty=(title,text,action="")=>`<div class="empty"><div class="empty-icon">＋</div><h3>${title}</h3><p>${text}</p>${action}</div>`;function dashboard(){const active=state.employees.filter((e=>e.active!==false)),today=state.attendance.filter((a=>a.date===date())),pending=state.leaveTransactions.filter((l=>l.status==="pending")),last=[...state.payrollRuns].filter((run=>run.status==="approved")).sort(((a,b)=>(b.createdAt||"").localeCompare(a.createdAt||"")))[0],summary=reportSummary();return head("HR & Payroll dashboard","A current view of your workforce and payroll operations.")+`<div class="report-toolbar"><label>Report period<input class="input" type="month" data-report-period value="${esc(reportPeriod)}" max="${month()}" aria-label="Report period"></label><span>Showing ${esc(summary.periodLabel)}</span><button class="btn secondary" data-export-report>Export summary Excel</button></div><div class="metrics"><div class="metric"><span>Active employees</span><strong>${active.length}</strong></div><div class="metric"><span>Present today</span><strong>${today.filter((a=>a.status==="present")).length}</strong></div><div class="metric warn"><span>Pending leave</span><strong>${pending.length}</strong></div><div class="metric"><span>Last payroll</span><strong>${last?money(last.totalNet):"—"}</strong></div></div><div class="report-grid dashboard-summary">${reportTable("Department Headcount",summary.departments,[["department","Department"],["count","Employees"]])}${reportTable("Leave Summary",summary.leaves,[["type","Leave Type"],["taken","Days Taken"]])}</div>`}function table(title,rows,cols,map=x=>x){return`<section class="panel"><div class="panel-head"><h2>${title}</h2></div>${rows.length?`<div class="table-wrap"><table><thead><tr>${cols.map((c=>`<th>${c[1]}</th>`)).join("")}</tr></thead><tbody>${rows.map((r=>{r=map(r);return`<tr>${cols.map((c=>`<td>${r[c[0]]??"—"}</td>`)).join("")}</tr>`})).join("")}</tbody></table></div>`:empty(`No ${title.toLowerCase()} yet`,`Create the first record to get started.`)}</section>`}const dept=id=>state.departments.find((d=>d.id===id)),emp=id=>state.employees.find((e=>e.id===id));function employees(){const countryOf=e=>employeeCountryCode(e,window.InfoBridgeCompany),counts=Object.fromEntries(["IN","AE"].map((country=>[country,state.employees.filter((e=>countryOf(e)===country)).length]))),filtered=state.employees.filter((e=>(employeeCountryFilter==="all"||countryOf(e)===employeeCountryFilter)&&(employeeDepartmentFilter==="all"||e.departmentId===employeeDepartmentFilter)&&(employeeStatusFilter==="all"||employeeStatusFilter==="active"===(e.active!==false))));const rows=filtered.map((e=>({...e,name:`${esc(e.firstName)} ${esc(e.lastName)}`,countryLabel:countryOf(e)==="AE"?"UAE":"India",department:dept(e.departmentId)?.name||e.departmentName||"—",salary:formatHrMoney(Number(e.basicSalary),countryOf(e)),workLocation:e.workLocation||"—",employmentStatus:e.employmentStatus||(e.active===false?"Inactive":"Active"),status:e.active===false?'<span class="badge neutral">Inactive</span>':'<span class="badge success">Active</span>',action:`<button class="text-button" data-edit-employee="${e.id}">Open</button>`})));const cards=`<section class="employee-country-actions"><article class="panel padded"><span class="badge neutral">INDIA</span><h2>India Employees</h2><p>${counts.IN} employee${counts.IN===1?"":"s"}</p><div class="quick-actions"><button class="btn primary" data-add-employee="IN">Add India Employee</button><button class="btn secondary" data-import-employees="IN">Import India Employees</button><button class="text-button" data-download-employee-template="IN">Download India Employee Template</button></div></article><article class="panel padded"><span class="badge neutral">UAE</span><h2>UAE Employees</h2><p>${counts.AE} employee${counts.AE===1?"":"s"}</p><div class="quick-actions"><button class="btn primary" data-add-employee="AE">Add UAE Employee</button><button class="btn secondary" data-import-employees="AE">Import UAE Employees</button><button class="text-button" data-download-employee-template="AE">Download UAE Employee Template</button></div></article></section>`;const filters=`<div class="filterbar"><label>Country<select class="input" data-employee-country-filter>${[["all","All"],["IN","India"],["AE","UAE"]].map((([value,label])=>`<option value="${value}" ${value===employeeCountryFilter?"selected":""}>${label}</option>`)).join("")}</select></label><label>Department<select class="input" data-employee-department-filter><option value="all">All departments</option>${state.departments.map((d=>`<option value="${d.id}" ${d.id===employeeDepartmentFilter?"selected":""}>${esc(d.name)}</option>`)).join("")}</select></label><label>Status<select class="input" data-employee-status-filter>${[["all","All"],["active","Active"],["inactive","Inactive"]].map((([value,label])=>`<option value="${value}" ${value===employeeStatusFilter?"selected":""}>${label}</option>`)).join("")}</select></label></div>`;return head("Employees","One employee master for India and UAE workforce records.")+cards+filters+table("Employee master",rows,[["employeeId","Employee ID"],["name","Employee Name"],["countryLabel","Country"],["department","Department"],["designation","Designation"],["employmentType","Employment Type"],["workLocation","Work Location"],["status","Status"],["salary","Basic Salary"],["action","Actions"]])}function employeeForm(e={},country="IN"){const c=resolveEmployeeCountryConfig(country),departments=[["","Select department"],...state.departments.map((d=>[d.id,d.name]))],basic=`<input type="hidden" name="countryCode" value="${c.country}"><section class="employee-form-section"><h3>Basic Information</h3><div class="form-grid">${field("employeeId","Employee ID","text",e.employeeId||`EMP-${String(state.employees.length+1).padStart(3,"0")}`,"required")}${field("firstName","First Name","text",e.firstName,"required")}${field("lastName","Last Name","text",e.lastName)}${field("email","Email","email",e.email)}${field("phone","Mobile","tel",e.phone)}${field("dateOfBirth","Date of Birth","date",e.dateOfBirth)}${select("gender","Gender",[["","Select"],["female","Female"],["male","Male"],["other","Other"],["prefer-not-to-say","Prefer not to say"]],e.gender)}${field("nationality","Nationality","text",e.nationality)}</div></section>`,location=c.country==="AE"?select("workLocation","Work Location / Emirate",[["","Select Emirate"],...c.jurisdictions.map((name=>[name,name]))],e.workLocation):field("workLocation","Work Location","text",e.workLocation),employment=`<section class="employee-form-section"><h3>Employment Details</h3><div class="form-grid">${select("departmentId","Department",departments,e.departmentId)}${field("designation","Designation","text",e.designation,"required")}${field("joiningDate","Date of Joining","date",e.joiningDate||date(),"required")}${select("employmentType","Employment Type",c.employmentTypes,e.employmentType)}${location}${select("employmentStatus","Employment Status",[["active","Active"],["probation","Probation"],["notice-period","Notice Period"],["inactive","Inactive"]],e.employmentStatus||"active")}</div></section>`,commonBank=`${select("salaryPaymentMethod","Salary Payment Method",[["bank-transfer","Bank Transfer"],["cash","Cash"],["cheque","Cheque"]],e.salaryPaymentMethod||"bank-transfer")}${field("bankName","Bank Name","text",e.bankName)}${field("accountHolderName","Account Holder Name","text",e.accountHolderName)}${field("bankAccount",c.country==="AE"?"Account Number":"Bank Account Number","text",e.bankAccount)}`;if(c.country==="IN")return basic+employment+`<section class="employee-form-section"><h3>Salary & Allowances — INR</h3><div class="form-grid">${field("basicSalary","Basic Salary (INR)","number",e.basicSalary||0,"min=0 step=.01 required")}${field("hra","HRA","number",e.hra||0,"min=0 step=.01")}${field("otherAllowances","Other Allowances","number",e.otherAllowances??e.allowances??0,"min=0 step=.01")}</div></section><section class="employee-form-section"><h3>India Statutory Information</h3><div class="form-grid">${field("pan","PAN","text",e.pan)}${field("uan","UAN","text",e.uan)}${select("pfApplicable","PF Applicable",[["false","No"],["true","Yes"]],String(e.pfApplicable??false))}${select("esiApplicable","ESI Applicable",[["false","No"],["true","Yes"]],String(e.esiApplicable??false))}${select("professionalTaxApplicable","Professional Tax Applicable",[["false","No"],["true","Yes"]],String(e.professionalTaxApplicable??false))}</div></section><section class="employee-form-section"><h3>Bank Details</h3><div class="form-grid">${commonBank}${field("ifsc","IFSC Code","text",e.ifsc)}</div></section>${weeklyOffFields(e)}`;return basic+employment+`<section class="employee-form-section"><h3>Salary & Allowances — AED</h3><div class="form-grid">${field("basicSalary","Basic Salary (AED)","number",e.basicSalary||0,"min=0 step=.01 required")}${field("housingAllowance","Housing Allowance","number",e.housingAllowance||0,"min=0 step=.01")}${field("transportAllowance","Transport Allowance","number",e.transportAllowance||0,"min=0 step=.01")}${field("otherAllowances","Other Allowances","number",e.otherAllowances??e.allowances??0,"min=0 step=.01")}</div></section><section class="employee-form-section"><h3>UAE Employment / Government Details</h3><div class="form-grid">${field("emiratesId","Emirates ID","text",e.emiratesId)}${field("workPermitNumber","Labour / Work Permit Number","text",e.workPermitNumber)}${field("visaNumber","Visa Number","text",e.visaNumber)}${field("visaExpiry","Visa Expiry Date","date",e.visaExpiry)}${field("workPermitExpiry","Work Permit Expiry Date","date",e.workPermitExpiry)}${field("wpsIdentifier","WPS / Payroll Identifier","text",e.wpsIdentifier)}</div></section><section class="employee-form-section"><h3>Bank Details</h3><div class="form-grid">${commonBank}${field("iban","IBAN","text",e.iban)}</div></section>${weeklyOffFields(e)}`}function openEmployee(id,country="IN",readOnly=false,recalculate=true){const old=state.employees.find((x=>x.id===id))||{},code=old.id?employeeCountryCode(old,window.InfoBridgeCompany):country;modal(readOnly?`${code==="AE"?"UAE":"India"} Employee`:old.id?`${code==="AE"?"UAE":"India"} Employee Profile`:`Add ${code==="AE"?"UAE":"India"} Employee`,employeeForm(old,code),(async(v,form)=>{if(readOnly)return;const weekly=validateWeeklyOff(v.weeklyOffType,new FormData(form).getAll("weeklyOffDays")),message=form.querySelector("[data-weekly-off-error]");message.textContent=weekly.errors.join(" ");if(!weekly.valid)throw Error(weekly.errors.join(" "));if(state.employees.some((e=>e.employeeId===v.employeeId&&e.id!==old.id)))throw Error("Employee ID already exists");const department=state.departments.find((d=>d.id===v.departmentId));if(!department||department.active===false&&v.departmentId!==old.departmentId)throw Error("Select an active department from Administration → Departments.");const record=prepareEmployeeRecord(old,{...v,departmentName:department.name,pfApplicable:v.pfApplicable==="true",esiApplicable:v.esiApplicable==="true",professionalTaxApplicable:v.professionalTaxApplicable==="true",weeklyOffType:v.weeklyOffType,weeklyOffDays:weekly.days},code,(()=>uid("EMP")));record.active=v.employmentStatus!=="inactive";await put("employees",record);if(recalculate)await recalculateDraftPayrolls()}),true);if(readOnly){$("#modal-root form")?.querySelectorAll("input,select,textarea").forEach((control=>control.disabled=true));const submit=$("#modal-root form button[type=submit]");if(submit)submit.hidden=true}}function downloadEmployeeTemplate(country=employeeImportCountry){const code=country==="AE"?"AE":"IN",data=employeeTemplateWorkbookData(code),title=`${code==="AE"?"UAE":"India"} Employee Import Template`,currencyColumns=code==="AE"?["Basic Salary AED","Housing Allowance","Transport Allowance","Other Allowances"]:["Basic Salary INR","HRA","Other Allowances"],{book:book}=brandedWorkbook(XLSX,{company:window.InfoBridgeCompany,country:code,title:title,headers:data.employees[0],currencyColumns:currencyColumns,sheetName:"Employees"});XLSX.writeFile(book,`employee-import-template-${code.toLowerCase()}.xlsx`)}function importSteps(country=employeeImportCountry){const label=country==="AE"?"UAE":"India";return`<ol class="import-steps"><li>Download ${label} Template</li><li>Fill Employee Details</li><li>Upload Completed File</li><li>Validate & Preview</li><li>Import Employees</li></ol><div class="quick-actions"><button class="btn secondary" type="button" data-download-employee-template="${country}">Download ${label} Employee Template</button><label class="btn secondary file-button">Upload completed ${label} template<input data-employee-import-file type="file" accept=".xlsx,.csv"></label></div><p class="form-note">The file is validated before anything is saved. Departments must already be active in Administration → Departments.</p>`}function bindEmployeeImportControls(){document.querySelectorAll("[data-download-employee-template]").forEach((button=>button.addEventListener("click",(()=>downloadEmployeeTemplate(button.dataset.downloadEmployeeTemplate||employeeImportCountry)))));$("[data-employee-import-file]")?.addEventListener("change",uploadEmployeeFile);$("[data-download-employee-errors]")?.addEventListener("click",(()=>download("employee-import-errors.csv",toCsv(employeeImportErrorRows(employeeImportResults),["Original row","Employee ID","Field","Validation error"]))))}function openEmployeeImport(country="IN"){employeeImportCountry=country==="AE"?"AE":"IN";employeeImportResults=[];modal(`Import ${employeeImportCountry==="AE"?"UAE":"India"} Employees`,importSteps(employeeImportCountry),(async()=>{throw Error("Upload and validate an employee file before importing.")}),true,"Import Valid Employees");bindEmployeeImportControls()}async function uploadEmployeeFile(event){const file=event.target.files[0];if(!file)return;try{if(!/\.(xlsx|csv)$/i.test(file.name))throw Error("Upload an XLSX or CSV employee template.");const datasets=await workbookDatasets(file),dataset=datasets.find((item=>item.sheetName.toLowerCase()==="employees"))||datasets[0];if(!dataset?.rows.length)throw Error("The Employees sheet contains no employee rows.");employeeImportResults=validateEmployeeImportRows(dataset.rows,{country:employeeImportCountry,departments:state.departments,existingEmployees:state.employees,createId:()=>uid("EMP")});showEmployeeImportPreview(file.name)}catch(error){toast(error.message,true);event.target.value=""}}function showEmployeeImportPreview(fileName){const valid=employeeImportResults.filter((row=>row.valid)),invalid=employeeImportResults.filter((row=>!row.valid)),duplicates=employeeImportResults.filter((row=>row.errors.some((error=>/duplicate|already exists/i.test(error.message))))),rows=employeeImportResults.map((row=>({employeeId:esc(row.values.employeeId),name:esc(`${row.values.firstName} ${row.values.lastName}`.trim()),department:esc(row.values.departmentName),designation:esc(row.values.designation),employmentType:esc(row.values.employmentType),salary:formatHrMoney(row.values.basicSalary,employeeImportCountry),status:esc(row.values.employmentStatus||"Active"),validation:row.valid?'<span class="badge success">Ready</span>':`<span class="badge danger">${esc(row.errors.map((error=>`Row ${error.rowNumber} — ${error.field}: ${error.message}`)).join(" · "))}</span>`})));const body=`${importSteps()}<div class="import-summary"><strong>${employeeImportResults.length} total rows</strong> · <span class="success-text">${valid.length} valid employees</span> · <span class="error-text">${invalid.length} rows with errors</span> · ${duplicates.length} duplicate Employee IDs</div>${table(`Import Preview — ${esc(fileName)}`,rows,[["employeeId","Employee ID"],["name","Employee Name"],["department","Department"],["designation","Designation"],["employmentType","Employment Type"],["salary","Basic Salary"],["status","Status"],["validation","Validation Result"]])}`;modal("Validate & preview employees",body,(async()=>{if(!valid.length)throw Error("There are no valid employees to import.");for(const row of valid)await put("employees",row.record);await load();render();showEmployeeImportResult(employeeImportResults.length,valid.length,invalid.length);return false}),true,"Import Valid Employees");bindEmployeeImportControls()}function showEmployeeImportResult(processed,imported,rejected){const errors=rejected?`<button class="btn secondary" type="button" data-download-employee-errors>Download Error Report</button>`:"";modal("Import completed",`<div class="import-summary"><strong>${processed} rows processed</strong><br><span class="success-text">${imported} employees imported</span><br><span class="error-text">${rejected} rows rejected</span></div><div class="quick-actions">${errors}</div>`,(async()=>{view="employees";location.hash="employees"}),false,"View Employees");bindEmployeeImportControls()}function activeAttendancePeriod(){if(!attendancePeriod.month)attendancePeriod.month=month();return{month:attendancePeriod.month,...monthPeriod(attendancePeriod.month)}}function attendanceWorkflow(){const p=activeAttendancePeriod(),employees=state.employees.filter((e=>e.active!==false)),summary=employees.map((e=>({employee:`<button type="button" class="text-button" data-open-employee-attendance="${e.id}">${esc(`${e.firstName} ${e.lastName}`)}</button>`,employeeCode:e.employeeId,...attendanceSummary(e,{attendance:state.attendance,leaves:state.leaveTransactions,settings:state.settings[0],from:p.from,to:p.to})})));return head("Attendance","Manual records and review-first Excel imports.",`<button class="btn secondary" data-attendance-template>Download template</button><label class="btn secondary file-button">Import Excel<input id="attendance-quick-file" type="file" accept=".xlsx,.csv"></label>`)+`<section class="panel padded"><div class="form-grid">${field("attendanceMonth","Attendance month","month",p.month,"required")}</div></section><section class="panel padded"><form id="attendance-manual-form"><div class="form-grid">${employeeIdSearchField()}${field("attendanceFrom","From date","date",p.from,"required")}${field("attendanceTo","To date","date",p.to,"required")}${select("status","Status",[["present","Present"],["paid-leave","Paid Leave"],["absent","Absent / LOP"],["half-day","Half Day"],["holiday","Holiday"],["week-off","Week Off"]])}${field("checkIn","Check in","time","09:30")}${field("checkOut","Check out","time","18:00")}${field("breakHours","Break hours","number",0,'min="0" step=".25"')}${field("overtimeHours","Overtime hours","number","",'min="0" step=".25"')}${field("remarks","Remarks")}<button class="btn primary" type="submit">Save attendance</button></div></form></section>`+table("Employee attendance summary",summary,[["employeeCode","ID"],["employee","Employee"],["calendarDays","Calendar days"],["workingDays","Working days"],["weeklyOffDays","Weekly off"],["presentDays","Present"],["paidLeaveDays","Paid leave"],["lopDays","Absent / LOP"],["workedHours","Worked hours"],["overtimeHours","OT hours"]])}function payrollWorkflow(){const rows=[...state.payrollRuns].sort(((a,b)=>b.createdAt.localeCompare(a.createdAt))).map((r=>({...r,total:money(r.totalNet),action:`<button class="text-button" data-open-run="${r.id}">View</button>${r.status==="draft"?` <button class="text-button" data-recalculate-run="${r.id}">Recalculate</button> <button class="text-button" data-delete-draft="${r.id}">Delete Draft</button> <button class="text-button" data-approve-run="${r.id}">Approve</button>`:""}`})));return head("Payroll","Attendance-based calculation, review, approval and locked payslip generation.",`<button class="btn primary" data-run-payroll>Run payroll</button>`)+table("Payroll runs",rows,[["period","Period"],["from","From"],["to","To"],["employeeCount","Employees"],["total","Net payroll"],["status","Status"],["action","Actions"]])}function runViewWorkflow(id){const r=state.payrollRuns.find((x=>x.id===id));if(!r)return payrollWorkflow();const rows=r.items.map((x=>({...x,earned:money(x.earnedBasic??x.basic),allowance:money(x.allowances),otPayDisplay:money(x.otPay??x.overtime),deductionDisplay:money((x.deductions||0)+(x.lopDeduction||0)),netDisplay:money(x.net),action:`<button class="text-button" data-payroll-breakdown="${r.id}:${x.employeeId}">Breakdown</button>`}))),attendanceIssues=payrollAttendanceIssues(r.items),otherIssues=r.items.filter((item=>(item.issues||[]).some((issue=>!/attendance day\(s\) missing/.test(issue)))));return head(`Payroll — ${r.period}`,`${esc(r.companyName||companyDisplayName())} · ${r.status.toUpperCase()} · ${r.from||""} to ${r.to||""}`,`<button class="btn secondary" data-back-payroll>Back</button>${r.status==="draft"?`<button class="btn primary" data-approve-run="${r.id}">Approve payroll</button>`:""}`)+(attendanceIssues.length||otherIssues.length?`<section class="panel padded"><p class="error-text">Payroll cannot be approved.${attendanceIssues.length?` Attendance is incomplete for ${attendanceIssues.length} employees.`:""}</p>${attendanceIssues.length?`<button class="btn secondary" data-view-attendance-issues="${r.id}">View attendance issues</button>`:""}<button class="btn secondary" data-go-attendance>Return to Attendance</button></section>`:"")+table("Payroll preview",rows,[["employeeCode","ID"],["name","Employee"],["calendarDays","Calendar days"],["weeklyOffDays","Weekly off"],["workingDays","Working days"],["presentDays","Present"],["paidLeaveDays","Paid leave"],["lopDays","LOP"],["weeklyOffWorkedDays","Weekly-off worked"],["workedHours","Worked hours"],["overtimeHours","OT hours"],["earned","Earned basic"],["allowance","Allowances"],["otPayDisplay","OT pay"],["deductionDisplay","Deductions"],["netDisplay","Net pay"],["action","Calculation"]])}function downloadAttendanceTemplate(){const headers=["Employee ID","Date","Status","Check-in","Check-out","Break Hours","Overtime Hours","Remarks"],sheet=XLSX.utils.aoa_to_sheet([headers,[state.employees[0]?.employeeId||"EMP-001",date(),"Present","09:30","18:00",1,0,""]]),book=XLSX.utils.book_new();XLSX.utils.book_append_sheet(book,sheet,"Attendance");XLSX.writeFile(book,"attendance-import-template.xlsx")}function showPayrollBreakdown(runId,employeeId){const run=state.payrollRuns.find((r=>r.id===runId)),item=run?.items.find((i=>i.employeeId===employeeId));if(!item)return;modal(`Calculation — ${item.name}`,`<div class="two-col"><dl><p><strong>Calendar days:</strong> ${item.calendarDays}</p><p><strong>Working days:</strong> ${item.workingDays}</p><p><strong>Weekly off:</strong> ${item.weeklyOffDays} (${esc(item.weeklyOffSnapshot?.days?.join(", ")||"—")})</p><p><strong>Present:</strong> ${item.presentDays}</p><p><strong>Paid leave:</strong> ${item.paidLeaveDays}</p><p><strong>LOP:</strong> ${item.lopDays}</p><p><strong>Missing:</strong> ${item.missingDays}</p><p><strong>Weekly-off worked:</strong> ${item.weeklyOffWorkedDays||0}</p><p><strong>Worked / OT hours:</strong> ${item.workedHours} / ${item.overtimeHours}</p></dl><dl><p><strong>Monthly basic:</strong> ${money(item.basic)}</p><p><strong>Per-day salary:</strong> ${money(item.perDay)}</p><p><strong>Earned basic:</strong> ${money(item.earnedBasic)}</p><p><strong>Allowances:</strong> ${money(item.allowances)}</p><p><strong>OT pay:</strong> ${money(item.otPay)}</p><p><strong>LOP deduction:</strong> ${money(item.lopDeduction)}</p><p><strong>Other deductions:</strong> ${money(item.deductions)}</p><p><strong>Net pay:</strong> ${money(item.net)}</p></dl></div>`,(async()=>{}),true)}const ATTENDANCE_STATUS_LABELS={present:"Present","paid-leave":"Paid Leave",absent:"Absent / LOP","half-day":"Half Day",holiday:"Holiday","week-off":"Week Off",missing:"Missing"};
const ATTENDANCE_STATUS_BADGE={present:"success","paid-leave":"success",absent:"danger","half-day":"warn",holiday:"neutral","week-off":"neutral",missing:"warn"};
const attendanceDayLabel=date=>{const[y,m,d]=date.split("-").map(Number);return`${String(d).padStart(2,"0")} ${new Intl.DateTimeFormat("en",{month:"short",timeZone:"UTC"}).format(new Date(Date.UTC(y,m-1,d)))}`};
// Employee Attendance Summary -> click an employee -> the complete calendar month for the
// currently selected Attendance period (never a hardcoded month), generated from
// attendanceMonthDetail so every date is shown, including dates with no attendance record
// ("Missing" -- never silently treated as Absent).
// The monthly attendance sheet is opened only from the Attendance page's own Employee
// Attendance Summary. Employee Master's Open action uses the separate employee profile.
// The sheet shares the Attendance page's current period (activeAttendancePeriod /
// attendancePeriod), so there is only one attendance-period concept.
function showEmployeeAttendanceDetail(employeeId){
  const employee=emp(employeeId);if(!employee)return;
  const p=activeAttendancePeriod(),rows=attendanceMonthDetail(employee,{attendance:state.attendance,leaves:state.leaveTransactions,settings:state.settings[0],from:p.from,to:p.to}),
    departmentName=dept(employee.departmentId)?.name||employee.departmentName||"—";
  const header=`<div class="form-grid attendance-sheet-header"><p><strong>Employee ID:</strong> ${esc(employee.employeeId)}</p><p><strong>Employee:</strong> ${esc(`${employee.firstName} ${employee.lastName}`)}</p><p><strong>Department:</strong> ${esc(departmentName)}</p><p><strong>Designation:</strong> ${esc(employee.designation||"—")}</p><label>Month<input class="input" type="month" data-attendance-sheet-month value="${esc(p.month)}"></label></div>`;
  const body=header+`<div class="table-wrap"><table><thead><tr><th>Date</th><th>Day</th><th>Status</th><th>Check-in</th><th>Check-out</th><th>Break hours</th><th>Worked hours</th><th>Overtime hours</th><th>Remarks</th><th></th></tr></thead><tbody>${rows.map((r=>`<tr><td>${attendanceDayLabel(r.date)}</td><td>${r.weekday}</td><td><span class="badge ${ATTENDANCE_STATUS_BADGE[r.status]||"neutral"}">${esc(ATTENDANCE_STATUS_LABELS[r.status]||r.status)}</span></td><td>${esc(r.checkIn||"—")}</td><td>${esc(r.checkOut||"—")}</td><td>${r.breakHours===""?"—":r.breakHours}</td><td>${r.hours===""?"—":r.hours}</td><td>${r.overtimeHours===""?"—":r.overtimeHours}</td><td>${esc(r.remarks||"—")}</td><td><button type="button" class="text-button" data-edit-attendance-day="${r.date}">${r.status==="missing"?"Add":"Edit"}</button></td></tr>`)).join("")}</tbody></table></div>`;
  modal(`${esc(`${employee.firstName} ${employee.lastName}`)} — ${reportMonthLabel(p.month)} Attendance`,body,(()=>false),"xl","Close");
  setTimeout((()=>{
    document.querySelectorAll("[data-edit-attendance-day]").forEach((button=>button.onclick=()=>showAttendanceDayEditor(employeeId,button.dataset.editAttendanceDay)));
    $("[data-attendance-sheet-month]")?.addEventListener("change",(event=>{if(!/^\d{4}-\d{2}$/.test(event.target.value))return;attendancePeriod={month:event.target.value};showEmployeeAttendanceDetail(employeeId)}));
  }),0);
}
// Both a Missing date and an already-recorded (possibly wrong) date open here. If a record
// already exists for this employee + date it is updated in place (same id); otherwise a new
// one is created -- there is never a second record for the same employee/date.
function showAttendanceDayEditor(employeeId,date){
  const employee=emp(employeeId);if(!employee)return;
  const existing=state.attendance.find((a=>attendanceBelongsToEmployee(a,employee)&&a.date===date));
  const body=`<p class="form-note">${esc(`${employee.firstName} ${employee.lastName}`)} · ${date}</p><div class="form-grid">${select("status","Status",[["present","Present"],["paid-leave","Paid Leave"],["absent","Absent / LOP"],["half-day","Half Day"],["holiday","Holiday"],["week-off","Week Off"]],existing?.status||"present")}${field("checkIn","Check in","time",existing?.checkIn||"09:30")}${field("checkOut","Check out","time",existing?.checkOut||"18:00")}${field("breakHours","Break hours","number",existing?.breakHours??0,'min="0" step=".25"')}${field("overtimeHours","Overtime hours","number",existing?.overtimeHours??"",'min="0" step=".25"')}${field("remarks","Remarks","text",existing?.remarks||"")}</div>`;
  modal(`${existing?"Edit":"Add"} attendance — ${date}`,body,(async values=>{
    const needsTime=["present","half-day"].includes(values.status);
    if(needsTime&&(!values.checkIn||!values.checkOut))throw Error("Check-in and Check-out are required for Present or Half Day");
    if(needsTime&&values.breakHours==="")throw Error("Break hours are required for Present or Half Day");
    const raw={employeeId:employee.id,date,status:values.status,checkIn:needsTime?values.checkIn:"",checkOut:needsTime?values.checkOut:"",breakHours:needsTime?+values.breakHours:0,overtimeHours:needsTime&&values.overtimeHours!==""?+values.overtimeHours:"",remarks:values.remarks||""},
      others=state.attendance.filter((a=>a.id!==existing?.id)),
      validation=validateAttendance(raw,{employees:state.employees,attendance:others,leaves:state.leaveTransactions,settings:state.settings[0]});
    if(!validation.valid)throw Error(validation.errors.join("; "));
    const record={...raw,...validation.facts,id:existing?.id||uid("ATT"),source:existing?.source||"manual",confirmed:true,createdAt:existing?.createdAt||(new Date).toISOString(),updatedAt:(new Date).toISOString()};
    await put("attendance",record);
    await recalculateDraftPayrolls();
  }),false,existing?"Save changes":"Save attendance");
}
// Employee ID search (CASE A: starting a manual entry from the main Attendance page). Never
// renders every employee in an open dropdown -- state.employees is already scoped to the
// current company only, so results can never include another company's employees.
function employeeIdSearchField(){return`<label class="employee-id-search full">Employee ID<input class="input" type="text" autocomplete="off" placeholder="Search Employee ID or name (e.g. SHY002)" data-employee-search><input type="hidden" name="employeeId" data-employee-search-value><ul class="employee-search-results" data-employee-search-results hidden></ul><p class="form-note" data-employee-search-selected hidden></p></label>`}
function employeeSearchMatches(query){
  const q=String(query||"").trim().toLowerCase();if(!q)return[];
  const pool=state.employees.filter((e=>e.active!==false)),byCode=pool.filter((e=>String(e.employeeId||"").toLowerCase().startsWith(q)));
  const byOther=pool.filter((e=>!byCode.includes(e)&&(String(e.employeeId||"").toLowerCase().includes(q)||`${e.firstName} ${e.lastName}`.toLowerCase().includes(q))));
  return[...byCode,...byOther].slice(0,10);
}
function bindEmployeeIdSearch(){
  const input=$("[data-employee-search]");if(!input)return;
  const hidden=$("[data-employee-search-value]"),results=$("[data-employee-search-results]"),selected=$("[data-employee-search-selected]");
  input.addEventListener("input",(()=>{
    hidden.value="";if(selected){selected.hidden=true;selected.textContent=""}
    const matches=employeeSearchMatches(input.value);
    if(!matches.length){results.hidden=true;results.innerHTML="";return}
    results.innerHTML=matches.map((e=>`<li data-employee-search-option="${e.id}">${esc(e.employeeId)} — ${esc(`${e.firstName} ${e.lastName}`)}</li>`)).join("");
    results.hidden=false;
  }));
  results.addEventListener("click",(event=>{
    const option=event.target.closest("[data-employee-search-option]");if(!option)return;
    const employee=emp(option.dataset.employeeSearchOption);if(!employee)return;
    hidden.value=employee.id;input.value=`${employee.employeeId} — ${employee.firstName} ${employee.lastName}`;
    results.hidden=true;results.innerHTML="";
    if(selected){selected.hidden=false;selected.textContent=`${dept(employee.departmentId)?.name||employee.departmentName||"—"} · ${employee.designation||"—"}`}
  }));
  input.addEventListener("blur",(()=>setTimeout((()=>{results.hidden=true}),150)));
}
function bindAttendancePayroll(){bindEmployeeIdSearch();document.querySelectorAll("[data-open-employee-attendance]").forEach((button=>button.onclick=()=>showEmployeeAttendanceDetail(button.dataset.openEmployeeAttendance)));$("[name=attendanceMonth]")?.addEventListener("change",(event=>{attendancePeriod={month:event.target.value};render()}));$("#attendance-manual-form")?.addEventListener("submit",(async event=>{event.preventDefault();try{const values=Object.fromEntries(new FormData(event.target)),from=values.attendanceFrom,to=values.attendanceTo;if(to<from)throw Error("To date must not be before From date");if(!values.employeeId)throw Error("Search and select an Employee ID");const employee=emp(values.employeeId);if(!employee)throw Error("Search and select an Employee ID");const period=periodCalendar(from,to,employee,state.settings[0]),dates=period.workingDates;if(!dates.length)throw Error("The selected range contains only weekly-off days");if(!confirm(`Save attendance?\nEmployee: ${employee.firstName} ${employee.lastName}\nDate range: ${from} to ${to}\nCalendar days: ${period.calendarDates.length}\nWorking days: ${dates.length}\nWeekly-off days skipped: ${period.weeklyOffDates.length}`))return;const needsTime=["present","half-day"].includes(values.status);if(needsTime&&(!values.checkIn||!values.checkOut))throw Error("Check-in and Check-out are required for Present or Half Day");if(needsTime&&values.breakHours==="")throw Error("Break hours are required for Present or Half Day");const batch=[],prepared=[];for(const day of dates){const raw={employeeId:values.employeeId,date:day,status:values.status,checkIn:needsTime?values.checkIn:"",checkOut:needsTime?values.checkOut:"",breakHours:needsTime?+values.breakHours:0,overtimeHours:needsTime&&values.overtimeHours!==""?+values.overtimeHours:"",remarks:values.remarks||""},validation=validateAttendance(raw,{employees:state.employees,attendance:state.attendance,leaves:state.leaveTransactions,settings:state.settings[0],batch:batch});if(!validation.valid)throw Error(`${day}: ${validation.errors.join("; ")}`);batch.push({...raw,employeeId:validation.employee.id});prepared.push({...raw,...validation.facts,id:uid("ATT"),employeeId:validation.employee.id,source:"manual",confirmed:true,createdAt:(new Date).toISOString()})}for(const record of prepared)await put("attendance",record);await recalculateDraftPayrolls();render();toast(`${prepared.length} attendance record(s) saved`)}catch(error){toast(error.message,true)}}));$("[data-attendance-template]")?.addEventListener("click",downloadAttendanceTemplate);$("#attendance-quick-file")?.addEventListener("change",importAttendance);$("[data-go-attendance]")?.addEventListener("click",(()=>{view="attendance";location.hash="attendance";render()}));document.querySelectorAll("[data-payroll-breakdown]").forEach((button=>button.onclick=()=>{const[runId,employeeId]=button.dataset.payrollBreakdown.split(":");showPayrollBreakdown(runId,employeeId)}))}function leaveEmployee(record){if(!record)return null;return emp(record.employeeId)||state.employees.find((employee=>String(employee.employeeId).toLowerCase()===String(record.employeeId||record.employeeCode).toLowerCase()))}function leaveLockedPayroll(record){return lockedPayrollForLeave(record,state.payrollRuns,leaveEmployee(record))}function beginLeaveEdit(id){const record=state.leaveTransactions.find((item=>item.id===id));if(!record)return;const locked=leaveLockedPayroll(record);if(locked){toast("This leave is linked to an approved payroll and cannot be edited. Historical payroll records are locked.",true);return}editingLeaveId=id;render();$("#leave-application-form")?.scrollIntoView({behavior:"smooth",block:"start"})}function openLeave(id){if(id)return beginLeaveEdit(id);editingLeaveId="";render()}function leaveMetrics(){const today=date(),currentMonth=today.slice(0,7),approved=state.leaveTransactions.filter((item=>item.status==="approved")),onLeave=new Set(approved.filter((item=>item.from<=today&&item.to>=today)).map((item=>item.employeeId))).size,pending=state.leaveTransactions.filter((item=>item.status==="pending")).length,approvedMonth=approved.filter((item=>item.from.slice(0,7)<=currentMonth&&item.to.slice(0,7)>=currentMonth)).length,unpaidMonth=approved.filter((item=>item.paid===false&&item.from.slice(0,7)<=currentMonth&&item.to.slice(0,7)>=currentMonth)).length;return`<div class="metrics"><div class="metric"><span>On Leave Today</span><strong>${onLeave}</strong></div><div class="metric warn"><span>Pending Requests</span><strong>${pending}</strong></div><div class="metric"><span>Approved This Month</span><strong>${approvedMonth}</strong></div><div class="metric"><span>Unpaid Leave This Month</span><strong>${unpaidMonth}</strong></div></div>`}function leaveApplicationCard(){const editing=state.leaveTransactions.find((item=>item.id===editingLeaveId)),selectedEmployee=leaveEmployee(editing),employees=state.employees.filter((employee=>employee.active!==false||employee.id===selectedEmployee?.id)),types=availableLeaveTypes(state.leaveTypes).filter((type=>leaveTypeIsActive(type)||type.id===editing?.leaveTypeId)),employeeValue=selectedEmployee?`${selectedEmployee.employeeId} — ${selectedEmployee.firstName} ${selectedEmployee.lastName}`:"";return`<section class="panel padded"><div class="panel-head"><h2>${editing?"Edit Leave Application":"Leave Application"}</h2>${editing?'<button class="text-button" type="button" data-cancel-leave-edit>Cancel Edit</button>':""}</div><form id="leave-application-form"><div class="form-grid"><label>Employee<input class="input" name="employeeSearch" list="leave-employee-options" value="${esc(employeeValue)}" placeholder="Search Employee ID or name" required><datalist id="leave-employee-options">${employees.map((employee=>`<option value="${esc(employee.employeeId)} — ${esc(employee.firstName)} ${esc(employee.lastName)}"></option>`)).join("")}</datalist></label>${select("leaveTypeId","Leave Type",types.map((type=>[type.id,type.name])),editing?.leaveTypeId||types[0]?.id)}<label data-other-leave-payment hidden>Other Leave Payment Type<select class="input" name="otherPaymentType"><option value="paid" ${editing?.paid!==false?"selected":""}>Paid</option><option value="unpaid" ${editing?.paid===false?"selected":""}>Unpaid</option></select></label>${field("from","From Date","date",editing?.from||date(),"required")}${field("to","To Date","date",editing?.to||date(),"required")}${field("days","Number of Leave Days","number",editing?.days||0,"readonly")}${field("reason","Reason / Notes","text",editing?.reason||editing?.notes||"","required")}${select("status","Status",[["pending","Pending"],["approved","Approved"],["rejected","Rejected"]],editing?.status||"pending")}<p class="form-note" data-selected-leave-balance></p><button class="btn primary" type="submit">${editing?"Update Leave":"Save"}</button></div></form></section>`}function leaveRequestsCard(){const query=leaveSearch.trim().toLowerCase(),filtered=state.leaveTransactions.filter((item=>leaveStatusFilter==="all"||item.status===leaveStatusFilter)).filter((item=>{const employee=leaveEmployee(item),text=`${employee?.employeeId||item.employeeCode||""} ${employee?.firstName||""} ${employee?.lastName||""}`.toLowerCase();return!query||text.includes(query)})).sort(((a,b)=>(b.createdAt||"").localeCompare(a.createdAt||""))),pageSize=10,pages=Math.max(1,Math.ceil(filtered.length/pageSize));leaveRequestPage=Math.min(leaveRequestPage,pages);const pageRows=filtered.slice((leaveRequestPage-1)*pageSize,leaveRequestPage*pageSize).map((item=>{const employee=leaveEmployee(item),status=`<span class="badge ${item.status==="approved"?"success":item.status==="rejected"?"danger":"neutral"}">${esc(item.status[0].toUpperCase()+item.status.slice(1))}</span>`,action=`<button class="text-button" data-edit-leave="${item.id}">Edit</button>`;return{employee:`${esc(employee?.employeeId||item.employeeCode||"")} — ${esc(employee?.firstName||"Unknown")} ${esc(employee?.lastName||"")}`,type:esc(item.leaveTypeName||"Leave"),dates:`${esc(item.from)} – ${esc(item.to)}`,days:item.days,status:status,action:action}}));return`<section class="panel padded"><div class="panel-head"><h2>Leave Requests</h2></div><div class="form-grid"><label>Search<input class="input" data-leave-search value="${esc(leaveSearch)}" placeholder="Employee Name / Employee ID"></label>${select("leaveStatusFilter","Filter",[["all","All"],["pending","Pending"],["approved","Approved"],["rejected","Rejected"]],leaveStatusFilter)}</div>${table("Leave requests",pageRows,[["employee","Employee"],["type","Leave Type"],["dates","Dates"],["days","Days"],["status","Status"],["action","Actions"]])}<div class="quick-actions"><button class="btn secondary" data-leave-page="${leaveRequestPage-1}" ${leaveRequestPage<=1?"disabled":""}>‹</button><span>Page ${leaveRequestPage} of ${pages}</span><button class="btn secondary" data-leave-page="${leaveRequestPage+1}" ${leaveRequestPage>=pages?"disabled":""}>›</button></div></section>`}function leave(){return head("Leave","Manage employee leave and approvals.")+leaveMetrics()+`<div class="two-col">${leaveApplicationCard()}${leaveRequestsCard()}</div>`}function selectedLeaveEmployee(form){const code=String(form.elements.employeeSearch?.value||"").split(" — ")[0].trim().toLowerCase();return state.employees.find((employee=>String(employee.employeeId).toLowerCase()===code||`${employee.firstName} ${employee.lastName}`.toLowerCase()===code))}function updateInlineLeaveForm(){const form=$("#leave-application-form");if(!form)return;const employee=selectedLeaveEmployee(form),type=availableLeaveTypes(state.leaveTypes).find((item=>item.id===form.elements.leaveTypeId.value)),other=form.querySelector("[data-other-leave-payment]"),hint=form.querySelector("[data-selected-leave-balance]");other.hidden=!type?.paymentSelectable;try{form.elements.days.value=employee?periodCalendar(form.elements.from.value,form.elements.to.value,employee,state.settings[0]).workingDates.length:0}catch{form.elements.days.value=0}if(!employee||!type){hint.textContent="";return}const balance=employeeLeaveBalances(employee,[type],state.leaveTransactions,state.settings[0])[0];hint.textContent=balance.allowed>0?`${type.name}: ${balance.available} days available`:""}async function saveInlineLeave(event){event.preventDefault();const form=event.currentTarget,values=Object.fromEntries(new FormData(form)),old=state.leaveTransactions.find((item=>item.id===editingLeaveId)),employee=selectedLeaveEmployee(form),baseType=availableLeaveTypes(state.leaveTypes).find((item=>item.id===values.leaveTypeId)),type=baseType?.paymentSelectable?{...baseType,paymentType:values.otherPaymentType}:baseType;if(values.to<values.from)throw Error("End date must not be before start date");const application={...old,...values,id:old?.id},validated=validateLeaveApplication({application:application,employee:employee,type:type,applications:state.leaveTransactions,settings:state.settings[0]}),candidate={...application,...validated,employeeId:employee.id,employeeCode:employee.employeeId,status:values.status||"pending"};if(lockedPayrollForLeave(candidate,state.payrollRuns,employee))throw Error("This leave is linked to an approved payroll and cannot be edited. Historical payroll records are locked.");if(state.attendance.some((row=>(row.employeeId===employee.id||row.employeeCode===employee.employeeId)&&validated.dates.includes(row.date))))throw Error("This leave conflicts with confirmed attendance");const now=(new Date).toISOString();await put("leaveTransactions",{...candidate,id:old?.id||uid("LEV"),createdAt:old?.createdAt||now,updatedAt:now});await recalculateDraftPayrolls();editingLeaveId="";leaveRequestPage=1;render();toast(old?"Leave application updated":"Leave application saved")}function bindLeavePage(){const form=$("#leave-application-form");if(form){form.onsubmit=async event=>{try{await saveInlineLeave(event)}catch(error){toast(error.message,true)}};["employeeSearch","leaveTypeId","from","to"].forEach((name=>form.elements[name]?.addEventListener("change",updateInlineLeaveForm)));updateInlineLeaveForm()}$("[data-cancel-leave-edit]")?.addEventListener("click",(()=>{editingLeaveId="";render()}));$("[data-leave-search]")?.addEventListener("change",(event=>{leaveSearch=event.target.value;leaveRequestPage=1;render()}));$("[name=leaveStatusFilter]")?.addEventListener("change",(event=>{leaveStatusFilter=event.target.value;leaveRequestPage=1;render()}));document.querySelectorAll("[data-leave-page]").forEach((button=>button.onclick=()=>{if(button.disabled)return;leaveRequestPage=Number(button.dataset.leavePage);render()}))}function openLeaveType(id){const old=state.leaveTypes.find((item=>item.id===id))||{};modal(old.id?"Edit Leave Type":"Add Leave Type",`<div class="form-grid">${field("name","Leave Name","text",old.name,"required")}${select("paymentType","Payment Type",[["paid","Paid"],["unpaid","Unpaid"]],old.paymentType||(old.paid===false?"unpaid":"paid"))}${field("allowedDays","Allowed Days","number",old.allowedDays??old.annualDays??0,"min=0 required")}${select("status","Status",[["active","Active"],["inactive","Inactive"]],old.status||(old.active===false?"inactive":"active"))}</div>`,(async values=>put("leaveTypes",{...old,...values,id:old.id||uid("LT"),allowedDays:+values.allowedDays,annualDays:+values.allowedDays,paid:values.paymentType==="paid",active:values.status==="active",updatedAt:(new Date).toISOString(),createdAt:old.createdAt||(new Date).toISOString()})))}function payroll(){const rows=[...state.payrollRuns].sort(((a,b)=>b.createdAt.localeCompare(a.createdAt))).map((r=>({...r,total:money(r.totalNet),action:`<button class="text-button" data-open-run="${r.id}">View</button>${r.status==="draft"?` <button class="text-button" data-approve-run="${r.id}">Approve</button>`:` <button class="text-button" data-reopen-run="${r.id}">Reopen</button>`}`})));return head("Payroll","Calculate, review, approve and safely reopen payroll runs.",`<button class="btn primary" data-run-payroll>Run payroll</button>`)+table("Payroll runs",rows,[["period","Period"],["employeeCount","Employees"],["total","Net payroll"],["status","Status"],["action","Actions"]])}function openPayroll(){const initial=canonicalPayrollPeriod(month());modal("Run payroll",`<div class="form-grid">${field("period","Payroll month","month",initial.period,"required")}${field("from","From date","date",initial.from,"readonly")}${field("to","To date","date",initial.to,"readonly")}</div><p class="form-note">The complete calendar month is used with confirmed attendance, approved leave and each employee's weekly-off configuration.</p>`,(async v=>{const{period:period,from:from,to:to}=canonicalPayrollPeriod(v.period);if(state.payrollRuns.some((r=>r.period===period&&r.status==="approved")))throw Error("An approved payroll run already exists for this month");const items=state.employees.filter((e=>e.active!==false)).map((e=>payrollForEmployee(e,{attendance:state.attendance,leaves:state.leaveTransactions,adjustments:state.payrollAdjustments,settings:state.settings[0],from:from,to:to})));await put("payrollRuns",{id:uid("PAY"),companyId:window.InfoBridgeCompany?.companyId||state.settings[0].companyId,companyName:companyDisplayName(),period:period,from:from,to:to,items:items,employeeCount:items.length,totalNet:items.reduce(((s,x)=>s+x.net),0),status:"draft",createdAt:(new Date).toISOString()})}));const periodInput=$("#modal-form [name=period]"),fromInput=$("#modal-form [name=from]"),toInput=$("#modal-form [name=to]");periodInput.addEventListener("change",(()=>{const dates=canonicalPayrollPeriod(periodInput.value);fromInput.value=dates.from;toInput.value=dates.to}))}function runView(id){const r=state.payrollRuns.find((x=>x.id===id));const rows=r.items.map((x=>({...x,basic:money(x.basic),allowances:money(x.allowances),overtime:money(x.overtime),deductions:money(x.deductions),net:money(x.net)})));return head(`Payroll — ${r.period}`,`${r.status.toUpperCase()} · ${r.employeeCount} employees`,`<button class="btn secondary" data-back-payroll>Back</button>${r.status==="draft"?`<button class="btn primary" data-approve-run="${r.id}">Approve payroll</button>`:""}`)+table("Payroll preview",rows,[["employeeCode","ID"],["name","Employee"],["basic","Basic"],["allowances","Allowances"],["overtime","Overtime"],["deductions","Deductions"],["net","Net pay"]])}function payslips(){const rows=state.payslips.map((p=>({...p,net:money(p.net),action:`<button class="text-button" data-print-slip="${p.id}">Print / download</button>`})));return head("Payslips","Printable payslips generated only from approved payroll.")+table("Generated payslips",rows,[["period","Period"],["employeeCode","Employee ID"],["name","Employee"],["calendarDays","Calendar days"],["workingDays","Working days"],["weeklyOffDays","Weekly off"],["presentDays","Present"],["paidLeaveDays","Paid leave"],["lopDays","LOP"],["overtimeHours","OT hours"],["weeklyOffWorkedDays","Weekly-off worked"],["net","Net pay"],["action","Actions"]])}function reportTable(title,rows,cols){return`<section class="panel report-card"><div class="panel-head"><h2>${title}</h2></div><div class="table-wrap"><table class="report-table"><thead><tr>${cols.map((column=>`<th>${column[1]}</th>`)).join("")}</tr></thead><tbody>${rows.map((row=>`<tr>${cols.map((column=>`<td>${esc(row[column[0]]??0)}</td>`)).join("")}</tr>`)).join("")}</tbody></table></div></section>`}function summaryForPeriod(period){return summarizeHrReport({period:period,employees:state.employees,departments:state.departments,attendance:state.attendance,payrollRuns:state.payrollRuns,leaveTypes:state.leaveTypes,leaveTransactions:state.leaveTransactions})}function reportSummary(){return summaryForPeriod(reportPeriod)}function reports(){return dashboard()}function settings(){const s=state.settings[0];return head("Settings & Backup","Company, attendance and local data controls.")+`<div class="two-col"><section class="panel padded"><h2>Company & payroll</h2><p><strong>${esc(companyDisplayName())}</strong> <a class="text-button" href="/app/admin.html">Edit in Administration</a><br>${esc(s.payFrequency)} payroll · day ${s.payrollDay}<br>Shift ${s.shiftStart}–${s.shiftEnd} · ${s.graceMinutes} min grace</p><button class="btn secondary" data-edit-settings>Edit settings</button></section><section class="panel padded"><h2>Local data privacy</h2><p>All HR and payroll records are stored in IndexedDB in this browser. Export regular backups before clearing browser data or changing devices.</p><button class="btn primary" data-backup>Download JSON backup</button> <label class="btn secondary file-button">Restore backup<input data-restore type="file" accept="application/json,.json"></label></section></div><section class="panel padded export-panel"><h2>Danger Zone</h2><p>Remove employee and workforce test records for this company without changing company configuration or other product data.</p><button class="btn danger-button" data-reset-employees>Reset Employee Data</button></section>`}async function openEmployeeReset(){const companyId=store?.company?.id||window.InfoBridgeCompany?.companyId;let status;try{status=await companySecurityStatus(companyId)}catch{modal("Secure reset unavailable",`<p>HR reset is disabled until the Company Security migration is available. No data was changed.</p>`,(async()=>false),false,"Close");return}if(!status.departmentModules.includes("hr_payroll")){modal("HR Reset Key required",`<p>The Company Owner must configure an HR & Payroll Reset Key in Company Security before employee data can be reset.</p><a class="btn secondary" href="/company-security.html">Open Company Security</a>`,(async()=>false),false,"Close");return}modal("Reset all employee data?",`<p class="error-text"><strong>This will permanently remove employees, attendance, leave, payroll runs and payslips for this company. Company Profile and other modules remain unchanged.</strong></p>${field("resetKey","HR Reset Key","password","",'autocomplete="off" required')}${field("confirmation","Type RESET EMPLOYEES to confirm","text","",'autocomplete="off" required')}`,(async values=>{if(values.confirmation!=="RESET EMPLOYEES")throw Error("Type RESET EMPLOYEES exactly to continue.");await resetEmployeeDataEverywhere({authorizeReset:async()=>{await executeHrReset(companyId,values.resetKey,values.confirmation);return{authorized:true,moduleId:"hr_payroll"}},createCloudStore:()=>createWorkspaceStore("hr-payroll"),createFallbackStore:localFallbackStore});await load();view="settings";$("#modal-root").innerHTML="";render();toast("Employee data reset successfully.");return false}),false,"Reset Employee Data");const input=document.querySelector('[name="confirmation"]'),button=$("#modal-form footer .primary");button.disabled=true;input.addEventListener("input",(()=>button.disabled=input.value!=="RESET EMPLOYEES"))}function render(){if(!state.settings?.length){$("#app").innerHTML=setup();bindSetup();return}let content=view.startsWith("run:")?runViewWorkflow(view.slice(4)):({dashboard:dashboard,employees:employees,attendance:attendanceWorkflow,leave:leave,payroll:payrollWorkflow,payslips:payslips,reports:reports,settings:settings}[view]||dashboard)();$("#app").innerHTML=shell(content);bind();bindAttendancePayroll();bindLeavePage();bindReports();document.querySelectorAll("[data-recalculate-run]").forEach((button=>button.onclick=()=>recalculateDraft(button.dataset.recalculateRun)));document.querySelectorAll("[data-delete-draft]").forEach((button=>button.onclick=()=>deleteDraft(button.dataset.deleteDraft)));document.querySelectorAll("[data-view-attendance-issues]").forEach((button=>button.onclick=()=>showAttendanceIssues(button.dataset.viewAttendanceIssues)));document.querySelectorAll("[data-edit-leave]").forEach((button=>button.onclick=()=>openLeave(button.dataset.editLeave)));document.querySelectorAll("[data-leave-status]").forEach((button=>button.onclick=async()=>{const[id,status]=button.dataset.leaveStatus.split(":"),old=state.leaveTransactions.find((item=>item.id===id)),employee=leaveEmployee(old),type=availableLeaveTypes(state.leaveTypes).find((item=>item.id===old.leaveTypeId)),validated=status==="approved"?validateLeaveApplication({application:{...old,status:status},employee:employee,type:type,applications:state.leaveTransactions,settings:state.settings[0]}):{days:old.days,dates:old.dates,paid:old.paid,leaveTypeName:old.leaveTypeName};await put("leaveTransactions",{...old,...validated,status:status,reviewedAt:(new Date).toISOString()});await recalculateDraftPayrolls();render()}));document.querySelectorAll("[data-import-employees]").forEach((button=>button.addEventListener("click",(()=>openEmployeeImport(button.dataset.importEmployees)))));document.querySelectorAll("[data-download-employee-template]").forEach((button=>button.addEventListener("click",(()=>downloadEmployeeTemplate(button.dataset.downloadEmployeeTemplate)))));$("[data-reset-employees]")?.addEventListener("click",openEmployeeReset)}function bindReports(){$("[data-report-period]")?.addEventListener("change",(event=>{reportPeriod=event.target.value||month();render()}));$("[data-export-report]")?.addEventListener("click",(()=>{const summary=reportSummary(),config=hrCountry();(()=>{const rows=[...summary.departments.map(row=>({Section:"Department Headcount",Item:row.department,Value:row.count})),...summary.leaves.map(row=>({Section:"Leave Summary",Item:row.type,Value:row.taken}))],headers=["Section","Item","Value"],{book}=brandedWorkbook(XLSX,{company:window.InfoBridgeCompany,country:config.country,title:`HR Summary — ${summary.periodLabel}`,headers:headers,rows:rows.map(row=>headers.map(header=>row[header])),sheetName:"HR Summary"});XLSX.writeFile(book,`hr-summary-${summary.period}.xlsx`)})()}))}function bindSetup(){$("#setup-form").onsubmit=async e=>{e.preventDefault();const v=Object.fromEntries(new FormData(e.target));await put("settings",{...v,id:"business",setupComplete:true,salaryDays:+v.salaryDays,payrollDay:+v.payrollDay,standardHours:+v.standardHours,graceMinutes:+v.graceMinutes,createdAt:(new Date).toISOString()});await load();render()}}function bind(){document.querySelectorAll("[data-view]").forEach((b=>b.onclick=()=>{view=b.dataset.view;render()}));$(".mobile-menu")?.addEventListener("click",(()=>$(".sidebar").classList.toggle("open")));document.querySelectorAll("[data-add-employee]").forEach((b=>b.onclick=()=>openEmployee(null,b.dataset.addEmployee)));document.querySelectorAll("[data-leave]").forEach((b=>b.onclick=openLeave));document.querySelectorAll("[data-run-payroll]").forEach((b=>b.onclick=openPayroll));document.querySelectorAll("[data-edit-employee]").forEach((b=>b.onclick=()=>openEmployee(b.dataset.editEmployee,"IN",false,false)));document.querySelectorAll("[data-view-employee]").forEach((b=>b.onclick=()=>openEmployee(b.dataset.viewEmployee,"IN",true)));$("[data-employee-country-filter]")?.addEventListener("change",(event=>{employeeCountryFilter=event.target.value;render()}));$("[data-employee-department-filter]")?.addEventListener("change",(event=>{employeeDepartmentFilter=event.target.value;render()}));$("[data-employee-status-filter]")?.addEventListener("change",(event=>{employeeStatusFilter=event.target.value;render()}));document.querySelectorAll("[data-toggle-employee]").forEach((b=>b.onclick=async()=>{const e=emp(b.dataset.toggleEmployee);await put("employees",{...e,active:e.active===false});await load();render()}));$("[data-leave-type]")?.addEventListener("click",(()=>modal("Add leave type",`<div class="form-grid">${field("name","Name","text","","required")}${field("annualDays","Annual days","number","12","min=0 required")}${select("paid","Pay treatment",[["true","Paid"],["false","Unpaid"]])}</div>`,(async v=>put("leaveTypes",{id:uid("LT"),name:v.name,annualDays:+v.annualDays,paid:v.paid==="true"})))));document.querySelectorAll("[data-leave-status]").forEach((b=>b.onclick=async()=>{const[id,status]=b.dataset.leaveStatus.split(":");await put("leaveTransactions",{...state.leaveTransactions.find((l=>l.id===id)),status:status,reviewedAt:(new Date).toISOString()});await load();render()}));document.querySelectorAll("[data-open-run]").forEach((b=>b.onclick=()=>{view=`run:${b.dataset.openRun}`;render()}));$("[data-back-payroll]")?.addEventListener("click",(()=>{view="payroll";render()}));document.querySelectorAll("[data-approve-run]").forEach((b=>b.onclick=()=>approve(b.dataset.approveRun)));document.querySelectorAll("[data-reopen-run]").forEach((b=>b.onclick=()=>reopen(b.dataset.reopenRun)));$("[data-report-csv]")?.addEventListener("click",(()=>download("hr-summary.csv",toCsv([{companyName:companyDisplayName(),activeEmployees:state.employees.filter((e=>e.active!==false)).length,attendanceRecords:state.attendance.length,approvedPayroll:state.payrollRuns.filter((r=>r.status==="approved")).reduce(((s,r)=>s+r.totalNet),0)}],["companyName","activeEmployees","attendanceRecords","approvedPayroll"]))));$("[data-backup]")?.addEventListener("click",backup);$("[data-restore]")?.addEventListener("change",restore);$("[data-edit-settings]")?.addEventListener("click",editSettings);document.querySelectorAll("[data-print-slip]").forEach((b=>b.onclick=()=>printSlip(b.dataset.printSlip)));$("#search")?.addEventListener("input",(e=>{const q=e.target.value.toLowerCase();document.querySelectorAll("tbody tr").forEach((r=>r.hidden=!r.textContent.toLowerCase().includes(q)))}))}function showAttendanceIssues(id){const run=state.payrollRuns.find((item=>item.id===id)),issues=payrollAttendanceIssues(run?.items);if(!run||!issues.length)return;modal("Attendance issues",table("Employees with incomplete attendance",issues,[["employeeId","Employee ID"],["employee","Employee"],["expectedWorkingDays","Expected working days"],["recordedDays","Recorded days"],["missingDays","Missing days"]]),(async()=>false),true,"Close")}async function recalculateDraft(id){const run=state.payrollRuns.find((item=>item.id===id));if(!run||run.status!=="draft")return;const{from:from,to:to}=canonicalPayrollPeriod(run.period),items=state.employees.filter((employee=>employee.active!==false)).map((employee=>payrollForEmployee(employee,{attendance:state.attendance,leaves:state.leaveTransactions,adjustments:state.payrollAdjustments,settings:state.settings[0],from:from,to:to})));await put("payrollRuns",{...run,from:from,to:to,items:items,employeeCount:items.length,totalNet:items.reduce(((sum,item)=>sum+item.net),0),recalculatedAt:(new Date).toISOString()});await load();render();toast("Draft payroll recalculated")}async function deleteDraft(id){const run=state.payrollRuns.find((item=>item.id===id));if(!run||run.status!=="draft"||!confirm(`Delete draft payroll for ${run.period}?`))return;for(const slip of state.payslips.filter((item=>item.runId===id)))await del("payslips",slip.id);await del("payrollRuns",id);await load();render();toast("Draft payroll deleted")}async function approve(id){const r=state.payrollRuns.find((x=>x.id===id));if(!r)return;const attendanceIssues=payrollAttendanceIssues(r.items),issues=payrollIssueSummary(r.items);if(issues){toast(`Payroll cannot be approved.${attendanceIssues.length?` Attendance is incomplete for ${attendanceIssues.length} employees.`:" Review payroll issues."}`,true);if(attendanceIssues.length)showAttendanceIssues(id);return}const duplicate=state.payrollRuns.find((run=>run.id!==id&&run.status==="approved"&&run.period===r.period&&run.items.some((item=>r.items.some((current=>current.employeeId===item.employeeId))))));if(duplicate){toast("Payroll cannot be approved because one or more employees are already approved for this month",true);return}if(!confirm(`Approve and lock payroll for ${r.period}? This generates payslips.`))return;try{const companyName=companyDisplayName(),companyProfileSnapshot=companyPayslipSnapshot(window.InfoBridgeCompany||{});await approvePayrollAndGeneratePayslips({store:store,run:r,companyName:companyName,companyProfileSnapshot:companyProfileSnapshot});
    // Push a Payroll Payment Batch into Finance Pending Review -- payroll approval never
    // touches Banking directly. Finance must still review/confirm the account and Post before
    // any Money Out is created (src/finance/app.js). Guarded so re-approving/reopening a run
    // already known to Finance never creates a second draft for the same run.
    try{
      const financeRepo=financeRepository(),financeState=financeRepo.load(),companyId=window.InfoBridgeCompany?.companyId||"",accounts=financeState.settings.defaultAccounts,alreadyKnown=financeState.journals.some((j=>j.source?.module==="Payroll"&&j.source?.entityId===r.id));
      if(companyId&&!alreadyKnown&&accounts.salaryPayable&&(accounts.bank||accounts.cash)&&financeState.accounts.some((a=>a.companyId===companyId))){
        const totalMinor=Math.round((Number(r.totalNet)||0)*100),glCash=accounts.bank||accounts.cash,draft=saveFinanceDraft(financeState,{companyId,branchId:"",postingDate:r.to||`${r.period}-01`,voucherType:"Payroll Payment Batch",referenceNumber:r.id,narration:`Payroll ${r.period} · ${r.employeeCount} employee(s)`,lines:[{accountId:accounts.salaryPayable,debitMinor:totalMinor,creditMinor:0,description:`Payroll ${r.period}`},{accountId:glCash,debitMinor:0,creditMinor:totalMinor,description:`Payroll ${r.period}`}],cashLink:{accountId:"",direction:"Out",amount:(Number(r.totalNet)||0),category:"Payroll Payment",sourceModule:"Payroll",sourceId:r.id,reference:r.period},source:{module:"Payroll",entityType:"Payroll Run",entityId:r.id,eventVersion:1,documentNumber:r.id,postingRuleId:"RULE-PAYROLL-PAYMENT",route:"/hr-payroll/index.html"}});
        financeRepo.save(draft.state);
      }
    }catch(financeError){console.warn("Could not create the Finance Payroll Payment Batch draft. Finance can still create it manually.",financeError)}
    await load();render();toast("Payroll approved, locked and payslips generated")}catch(error){console.error("Payroll approval failed.",error);await load();render();toast(`Payroll approval failed: ${error.message}`,true)}}async function reopen(id){const reason=prompt("Reason for reopening this approved payroll:");if(!reason)return;const r=state.payrollRuns.find((x=>x.id===id));await put("payrollRuns",{...r,status:"draft",reopenReason:reason,reopenedAt:(new Date).toISOString()});for(const p of state.payslips.filter((p=>p.runId===id)))await del("payslips",p.id);await load();render();toast("Payroll reopened; generated payslips were withdrawn")}function printSlip(id){const p=state.payslips.find((x=>x.id===id));if(!p)return;const run=state.payrollRuns.find((item=>item.id===p.runId)),printRecord=p.companyProfileSnapshot?p:{...p,companyProfileSnapshot:run?.companyProfileSnapshot},employee=emp(p.employeeId)||state.employees.find((x=>x.employeeId===p.employeeCode))||{},departmentName=dept(employee.departmentId)?.name||employee.departmentName||"",w=open("","_blank");if(!w){toast("Allow pop-ups to open the payslip print view",true);return}w.document.open();w.document.write(renderPayslipDocument({payslip:printRecord,employee:employee,company:window.InfoBridgeCompany||{},departmentName:departmentName}));w.document.close()}async function importAttendance(e){const file=e.target.files[0];if(!file)return;try{const datasets=await workbookDatasets(file),parsed=datasets.flatMap((sheet=>sheet.rows));if(!parsed.length)throw Error("The attendance file has no data rows");const headers=Object.keys(parsed[0]||{}),guess=(...names)=>headers.find((h=>names.includes(h.toLowerCase())))||"",opts=(sel="")=>`<option value="">Do not import</option>${headers.map((h=>`<option ${h===sel?"selected":""}>${esc(h)}</option>`)).join("")}`;modal("Map attendance Excel",`<p>Map the columns, then preview every row before import.</p><div class="form-grid"><label>Employee ID<select class="input" name="employeeId" required>${opts(guess("employee id","employeeid","emp id"))}</select></label><label>Date<select class="input" name="date" required>${opts(guess("date","attendance date"))}</select></label><label>Status<select class="input" name="status" required>${opts(guess("status","attendance status"))}</select></label><label>Check in<select class="input" name="checkIn">${opts(guess("check-in","check in","checkin","in"))}</select></label><label>Check out<select class="input" name="checkOut">${opts(guess("check-out","check out","checkout","out"))}</select></label><label>Break hours<select class="input" name="breakHours">${opts(guess("break hours","break"))}</select></label><label>Overtime hours<select class="input" name="overtimeHours">${opts(guess("overtime hours","ot hours","overtime"))}</select></label><label>Remarks<select class="input" name="remarks">${opts(guess("remarks","notes"))}</select></label></div>${field("mappingName","Save mapping as","text",file.name.replace(/\.(csv|xlsx)$/i,""))}`,(async map=>{await put("attendanceMappings",{id:uid("MAP"),name:map.mappingName||file.name,map:map,createdAt:(new Date).toISOString()});const batch=[];importRows=parsed.map(((row,index)=>{let rawDate=String(row[map.date]||"").trim();if(/^\d{5}(\.\d+)?$/.test(rawDate)){const d=new Date(Date.UTC(1899,11,30)+Number(rawDate)*864e5);rawDate=d.toISOString().slice(0,10)}const raw={employeeId:row[map.employeeId],date:rawDate,status:String(row[map.status]||"present").trim().toLowerCase().replaceAll(" ","-"),checkIn:row[map.checkIn]||"",checkOut:row[map.checkOut]||"",breakHours:+row[map.breakHours]||0,overtimeHours:row[map.overtimeHours]===""?"":+row[map.overtimeHours],remarks:row[map.remarks]||""},validation=validateAttendance(raw,{employees:state.employees,attendance:state.attendance,leaves:state.leaveTransactions,settings:state.settings[0],batch:batch});if(validation.valid)batch.push({...raw,employeeId:validation.employee.id});return{index:index+1,raw:raw,...validation}}));reviewImport(file.name);return false}),true)}catch(error){toast(error.message,true);e.target.value=""}}function editSettings(){const s=state.settings[0];modal("Edit HR & payroll settings",`<div class="form-grid"><label>Company<input class="input" value="${esc(companyDisplayName())}" readonly></label><p class="form-note"><a href="/app/admin.html">Edit company name in Administration</a></p>${field("payrollDay","Payroll day","number",s.payrollDay,"min=1 max=31 required")}${field("salaryDays","Salary divisor days","number",s.salaryDays,"min=1 required")}${field("shiftStart","Shift start","time",s.shiftStart,"required")}${field("shiftEnd","Shift end","time",s.shiftEnd,"required")}${field("standardHours","Daily hours","number",s.standardHours,"min=1 step=.25 required")}${field("graceMinutes","Grace minutes","number",s.graceMinutes,"min=0")}</div>`,(async v=>put("settings",{...s,...v,payrollDay:+v.payrollDay,salaryDays:+v.salaryDays,standardHours:+v.standardHours,graceMinutes:+v.graceMinutes,updatedAt:(new Date).toISOString()})))}function reviewImport(name){const valid=importRows.filter((r=>r.valid)),bad=importRows.filter((r=>!r.valid));modal("Review attendance import",`<div class="import-summary"><strong>${valid.length} valid</strong> · <span class="error-text">${bad.length} blocked</span>. Only valid rows can be confirmed.</div><p class="form-note" data-attendance-import-progress aria-live="polite"></p><div class="table-wrap"><table><thead><tr><th>Row</th><th>Employee</th><th>Date</th><th>Time</th><th>Validation</th></tr></thead><tbody>${importRows.map((r=>`<tr><td>${r.index}</td><td>${esc(r.raw.employeeId)}</td><td>${esc(r.raw.date)}</td><td>${esc(r.raw.checkIn)}–${esc(r.raw.checkOut)}</td><td>${r.valid?`<span class="badge success">Valid</span> ${esc(r.warnings.join("; "))}`:`<span class="badge danger">Blocked</span> ${esc(r.errors.join("; "))}`}</td></tr>`)).join("")}</tbody></table></div>`,(async(_,form)=>{if(!valid.length)throw Error("There are no valid rows to confirm");const controls=attendanceImportProgressController(form,valid.length);controls.start();try{const result=await saveAttendanceImport({store:store,valid:valid,bad:bad,fileName:name,createId:uid,onProgress:saved=>controls.update(saved)});await recalculateDraftPayrolls();$("#modal-root").innerHTML="";render();toast(`${result.saved} attendance records imported successfully.`);return false}catch(error){controls.fail(error,Number(error.saved||0));throw error}}),true,"Save attendance")}async function backup(){const data={companyId:window.InfoBridgeCompany?.companyId||state.settings[0]?.companyId,companyName:companyDisplayName(),schema:"InfoBridgeIndiaHRPayroll",version:1,exportedAt:(new Date).toISOString(),stores:Object.fromEntries(STORES.map((s=>[s,state[s]])))};download(`hr-payroll-backup-${date()}.json`,JSON.stringify(data,null,2),"application/json")}async function restore(e){const file=e.target.files[0];if(!file)return;try{const data=JSON.parse(await file.text());if(data.schema!=="InfoBridgeIndiaHRPayroll"||!data.stores?.settings?.[0]?.setupComplete)throw Error("This is not a valid HR & Payroll backup");if(!confirm("Replace all HR & Payroll data with this backup?"))return;for(const s of STORES)await store.replace(s,data.stores[s]||[]);await load();render();toast("Backup restored successfully")}catch(x){toast(x.message,true)}}window.addEventListener("error",(e=>{console.error(e.error);toast("Something went wrong. Your saved data was not removed.",true)}));async function start(){try{await openDb();await recoverPersistedFallback();await migrateDepartments();await load();await ensureHrSettings();await repairStoredDraftPayrollPeriods();render();window.InfoBridgeHrBootDone?.()}catch(error){console.error("HR & Payroll startup failed.",error);window.InfoBridgeHrBootFailed?.(error)}}listenForAdministrationCompany((()=>{state.departments=sharedDepartments({includeIds:(state.employees||[]).map((employee=>employee.departmentId))});if(state.settings?.length)render()}));start();
