import{acceptCompanyAdminInvitation}from"/company/admin-invitations.js";
import{clearCompanyCache,currentCompany,currentUser}from"/supabase/client.js";
const params=new URLSearchParams(location.search),token=params.get("token")||"",intro=document.querySelector("[data-invite-intro]"),error=document.querySelector("[data-invite-error]"),success=document.querySelector("[data-invite-success]"),authActions=document.querySelector("[data-invite-auth-actions]"),acceptPanel=document.querySelector("[data-invite-accept]"),acceptButton=document.querySelector("[data-accept-company-admin]");
const showError=message=>{error.textContent=message;error.hidden=false};
if(!token){intro.textContent="This invitation link is incomplete.";showError("Invitation token is missing.")}else{const user=await currentUser();if(!user){intro.textContent="Authenticate with the invited email address to continue.";authActions.hidden=false;const encoded=encodeURIComponent(token);document.querySelector("[data-invite-login]").href=`/login.html?invite=${encoded}`;document.querySelector("[data-invite-signup]").href=`/signup.html?invite=${encoded}`}else{intro.textContent="Confirm that you want to join this company as Company Admin.";acceptPanel.hidden=false;document.querySelector("[data-invite-email]").textContent=user.email||"authenticated account";acceptButton.onclick=async()=>{acceptButton.disabled=true;error.hidden=true;try{await acceptCompanyAdminInvitation(token);clearCompanyCache();const company=await currentCompany();success.textContent=`Invitation accepted. You joined ${company?.name||"the company"} as Company Admin.`;success.hidden=false;acceptPanel.hidden=true;setTimeout(()=>location.replace("/index.html"),900)}catch(cause){showError(cause.message||"Unable to accept this invitation.");acceptButton.disabled=false}}}}
