import { resolveCountryConfig } from "../country/registry.js";
const value=(row,...names)=>names.map(n=>row[n]).find(v=>String(v??"").trim())??"";
export function supplierImportSchema(company){const c=resolveCountryConfig(company),ae=c.country==="AE";return{country:c.country,identifierLabel:c.tax.identifier,regionLabel:c.jurisdictionLabel,columns:ae?["Type","Name","Mobile","Email","TRN","Emirate"]:["Type","Name","Mobile","Email","GSTIN","State Code"]}}
export function supplierTemplateCsv(company){return`${supplierImportSchema(company).columns.join(",")}\r\n`}
export function validateSupplierImportRows(rows,existing=[],company){const c=resolveCountryConfig(company),ae=c.country==="AE";return rows.map((row,index)=>{const identifier=String(value(row,ae?"TRN":"GSTIN",ae?"GSTIN":"TRN")).trim(),region=String(value(row,ae?"Emirate":"State Code",ae?"State":"Emirate")).trim(),mobile=String(value(row,"Mobile")).trim(),errors=[];if(!value(row,"Name"))errors.push("Name required");if(!mobile)errors.push("Mobile required");if(!region)errors.push(`${c.jurisdictionLabel} required`);if(identifier&&!c.tax.validateIdentifier(identifier))errors.push(`Invalid ${c.tax.identifier}`);if(existing.some(x=>x.mobile===mobile||identifier&&String(x.trn||x.gstin||"").toLowerCase()===identifier.toLowerCase()))errors.push("Duplicate supplier");return{row,index:index+2,identifier,region,mobile,errors,data:{type:value(row,"Type")||"Business",name:value(row,"Name"),mobile,email:value(row,"Email"),[c.tax.identifierField]:identifier,state:ae?region:value(row,"State"),stateCode:region,active:true}}})}
